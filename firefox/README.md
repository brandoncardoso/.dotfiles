# Firefox

## user.js

Configuration file for Firefox settings.

1. Find profile folder path by opening `about:support` in Firefox, under `Application Basics` > `Profile Folder`
2. Create link to `user.js`: `ln -s user.js <profile folder>/user.js`
